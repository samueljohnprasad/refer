--- src/components/ui/Timeline/Timeline.tsx
+++ src/components/ui/Timeline/Timeline.tsx
@@ -103,7 +103,7 @@
               <TimelineStemLine
                 flex={true}
                 style={{
-                  marginTop: isVeryFirst ? 14 : 0,
+                  marginTop: isVeryFirst ? 24 : 0,
                   marginBottom: isVeryLast ? 24 : 0,
                 }}
               />
@@ -113,7 +113,7 @@
             {isFirstItemInSection && (
               <View
                 style={[
-                  styles.absoluteDot,
+                  styles.absoluteDot, // top: 22
                   { backgroundColor, borderRadius: 10 },
                 ]}
               >
@@ -183,7 +183,7 @@
   dateColumn: {
-    width: 44,
-    alignItems: "center",
-    paddingTop: 14, // align with card vertically
+    width: 52,
+    alignItems: "flex-end", // right align date against the stem
+    paddingTop: 16, // align with card vertically
+    paddingRight: 12,
   },
 
   // ── Stem column ──────────────────────────────────────────────────────
@@ -195,7 +195,7 @@
   },
   absoluteDot: {
     position: "absolute",
-    top: 14, // align with date text
+    top: 22, // align with date text center
   },
 
   // ── Card column ──────────────────────────────────────────────────────

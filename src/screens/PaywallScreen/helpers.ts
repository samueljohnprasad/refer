export const COLORS = {
  // 5-color palette total to match the screenshot
  primary: "#8B8DF4", // purple header / filled plan
  accent: "#F6C24B", // yellow CTA + badge
  white: "#FFFFFF",
  text: "#0F172A", // near-slate-900 for headings
  // All borders/muted text use alpha of text to avoid adding more colors
};
export const RADIUS = {
  outer: 28,
  inner: 22,
  chip: 18,
  tile: 16,
  pill: 16,
};
